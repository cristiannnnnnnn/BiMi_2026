```
   uv sync # resolves all dependencies
   uv run python .... # running a python file in that environment
```
uv creates a .venv, so you can source it and use pyhton like you normally would if you want to.